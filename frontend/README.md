## Frontend (Next.js)

### Env var
- `NEXT_PUBLIC_BACKEND_URL` (default `http://localhost:8000`)

### Local .env
Create `frontend/.env.local` (not committed) based on `frontend/env.local.example`.

### Run
```bash
cd frontend
npm install
npm run dev
```

Open `http://localhost:3000`.


